# Automatic material 003 — Make ten first

Isolated branch: preview/mission-auto-make-ten-20260924. Parent: 9a458d2da117ee176439148b92db46e497f5fe0c, which descends from the user's verified fullscreen commit f1a3e1cb2ee2cda188e24044cccd18e938033a11. No production promotion.

Read-only comparison found the fullscreen and group branches unchanged at f1a3e1c and d13f132 respectively, and main unchanged at 3619368. The Count on branch already supplies automatic material 002; it is retained. No additional Make ten automatic material was present in the remote mission sources. Existing older dirty local checkouts are not edited.

Scope is the generated bridge-ten question only (Make ten first / Former dix d’abord). Missing number to ten remains a separate future increment. The existing question generator, order, lesson session, 40-minute adjustable default, avatar, microphone and AI endpoint are unchanged.

makeTenData(step) connects arithmetic, stable counter identities, movement, equation, EN/FR explanation and AI context. Before answering, two ten-frames show the actual green and gold counters, with total hidden. After an answer or explicit solution request, the required gold counters move into empty first-frame cells. The remaining gold counters stay beside the filled frame. No counter is created or removed. One observation and one solution appear automatically; no new media controls, streams, remote images or API calls are added.

The controller follows the Count on pause and persistence model: input/microphone/dialogue suspend the same animation frame, normal dialogue return or Continue resumes it, teacher pauses remain paused, language changes only the labels, and reload restores the saved lesson paused. The material key includes the existing session ID and numbers. New lesson creates a fresh session using the unchanged generator; Next retains it. Storage remains same-origin/device/browser only.

Build: build-make-ten.mjs imports the entire build-count-on.mjs chain (including build-fullscreen-exit.mjs) before applying exact-anchor integration to final output. Every controller is synchronized before the selected-material early return, so earlier JOIN/Count on visuals deactivate correctly. The service worker includes the new local module with a fresh cache name. All earlier source files are retained; package.json selects the new build entry.

Validation: pure tests cover all 45 supported number pairs, 945 intermediate frames, conservation, final ten-frame occupancy, unique positions, EN/FR equations and non-mutating data. The final browser suite uses the real existing VRM and simulated speech/recognition, checks numeric questions without grading, no pre-answer solution, correction hold, interrupted animation and dialogue return, teacher-paused return, language/reload, receipts, mobile layouts, fullscreen exit and old-material reactivation. Existing dialogue/fullscreen suites run once more on the final extended classroom. Build reports distinguish live provider checks from mocked speech and absent credentials.

These are automatic checks, not physical Samsung/iPhone microphone/TTS tests or full-duration classroom trials. No guarantee of all AI responses, offline free dialogue or cross-device synchronization is made. All new diagrams are original; no external candidate resource or reuse permission is assumed.
